[tool.poetry]
name = "qpu-monitoring"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9,<3.12"
qibocal = {git = "https://github.com/qiboteam/qibocal.git", branch = "main" }
prometheus-client = "^0.20.0"
jinja2 = "^3.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
